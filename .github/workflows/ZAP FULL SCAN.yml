name: Test File Creation and Upload

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test_write:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout code
      - name: Checkout code
        uses: actions/checkout@v2

      # Step 2: Pull ZAP Docker Image
      - name: Pull ZAP Docker Image
        run: |
          docker pull ghcr.io/zaproxy/zaproxy:stable

      - name: Run ZAP Full Scan
        run: |
          # Ensure the mounted directory has the correct permissions
          chmod -R 777 $(pwd)/zap_report.html
          
          # Run the ZAP Full Scan while excluding .git directory and other irrelevant files
          docker run \
            --network="host" \
            -v $(pwd):/zap/wrk/:rw \
            ghcr.io/zaproxy/zaproxy:stable \
            sh -c "find /zap/wrk -type d -not -name .git -exec chmod 777 {} + && zap-full-scan.py -t https://example.com -r /zap/wrk/zap_report.html && chmod 666 /zap/wrk/zap_report.html"


      # Step 4: Upload Test Report to Azure Blob Storage
      - name: Upload Test Report to Azure
        run: |
          az storage blob upload --account-name atlantisdevstorage001 \
          --container-name ramdev001 \
          --name path/to/report/zap_test_report.html \
          --file zap_test_report.html \
          --account-key ${{ secrets.AZURE_STORAGE_KEY }}
